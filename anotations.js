"use strict";
let movie_name = "SpiderMan brand new day";
let ticket = 1000;
let avaliableSeats = true;
function bookingTickets(hero, seats) {
    return `${hero} hits the massive blockbusterhit and there is only ${seats} available`;
}
let summary = bookingTickets(movie_name, ticket);
console.log(summary);
let Arr = ["hi", "hello", "namastey"];
console.log(Arr);
