let movie_name:string="SpiderMan brand new day";
let ticket:number=1000;
let avaliableSeats:boolean=true;

function bookingTickets(hero:String,seats:number): string{
return `${hero} hits the massive blockbusterhit and there is only ${seats} available`;

}
let summary:string=bookingTickets(movie_name,ticket);
console.log(summary);
 let Arr:string[]=["hi","hello","namastey"];
 console.log(Arr);
